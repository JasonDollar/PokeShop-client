import React from 'react'
import LoadingStyles from './styles/Loading'

const Loading = () => (
  <LoadingStyles>
    <span>.</span>
    <span>.</span>
    <span>.</span>
  </LoadingStyles>
)

export default Loading
